{
  "name": "sistema-agua",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p backend/public && cp -R frontend/out/. backend/public/",
    "start": "cd backend && node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}