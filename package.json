{
  "name": "sistema-agua",
  "version": "1.0.0",
  "description": "Sistema de Agua - Frontend + Backend unificado",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r out/* ../backend/public/ && cd ../backend && npm install",
    "start": "cd backend && node src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}